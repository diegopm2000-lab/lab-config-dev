spring:
  config:
    import:
      - "optional:configserver:${CONFIG_REMOTE_FILE}"
      - "optional:configserver:application.yml"
  cloud:
    config:
      enabled: true
      server:
        git:
          uri: https://github.com/your-repo/config-repo
      
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: admin
    password: alfa1beta2
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2-console
